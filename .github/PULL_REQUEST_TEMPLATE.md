<!-- Keep this focused. Delete sections that don't apply. -->

## What & why

<!-- What changes, and the reason for it. Link the issue: Closes #000 -->

## How it was verified

<!-- Commands run, tests added/passing, manual checks, screenshots -->

## Checklist

- [ ] Change is scoped to one concern; unrelated edits split out
- [ ] Tests and docs updated where behaviour changed
- [ ] `@authormark` headers left intact (refresh a stale fingerprint with `authormark stamp <file>`, never delete it)
- [ ] CI is green
